# Thought 011: StateManager Sınıfı — Temel Yapı

## Chain
chain_002_state

## Katman
Worker

## Input
"StateManager sınıfının temel yapısını oluştur — constructor, current(), transition()"

## Context Refs
- t_005 (SystemState, VALID_TRANSITIONS)
- t_008 (ForemanState, StateTransition)
- ARCHITECTURE.md → State Machine

## Muhakeme
StateManager, Foreman'ın disiplin mekanizması. Her state değişikliği
bu sınıf üzerinden geçmeli — doğrudan state manipülasyonu yasak.

Tasarım kararları:

1. Sınıf mı, fonksiyon mı?
   → Sınıf. State + history birlikte tutulmalı. Encapsulation önemli.
   
2. Constructor ne almalı?
   → Mevcut ForemanState. Ya dosyadan okunur (resume) ya da 
   fresh oluşturulur (yeni proje).
   
3. transition() ne döndürmeli?
   → void yerine yeni state döndürmeli. Immutable pattern.
   Hata durumunda exception fırlatır.

4. Thread safety?
   → Node.js single-threaded, gerekmez. Ama concurrent 
   çağrıları engellemek için bir "locked" flag olabilir.
   → Şimdilik basit tutalım, sonra ekleriz.

5. transition() imzası:
   → transition(to: SystemState, reason: string, context?: {...})
   → reason ZORUNLU. Neden bu geçişi yapıyoruz her zaman bilinmeli.

## Output
src/state.ts oluşturuldu — StateManager temel yapısı

## Confidence
0.9

## Verification
- npx tsx src/state.ts hatasız
- StateManager oluşturulabilir
- current() mevcut state döndürür

## Status
done
