# Thought 019: LLM Provider Abstraction

## Chain
chain_005_engine

## Katman
Worker

## Input
"Çoklu LLM provider'ı (Anthropic, OpenAI, Google) tek arayüzle kullanabilmek için provider abstraction yaz"

## Context Refs
- t_001 (Layer — her katmanın farklı modeli var)
- t_007 (RateLimitConfig — model rotasyonu)
- ARCHITECTURE.md → model kararları

## Muhakeme
Engine LLM çağrısı yaparken hangi provider'ı kullandığını bilmemeli.
Tek bir interface: `generate(prompt, model) → response`

Bu ilk aşamada gerçek LLM çağrısı yapmam gerekmiyor.
Mock provider ile tüm sistemi test edebilirim.
Gerçek provider'lar sonra eklenir.

Provider interface:
- generate(messages, options) → GenerateResult
- messages: { role, content }[]
- options: { model, maxTokens, temperature }
- result: { text, tokenUsage, model }

Mock provider:
- Her zaman sabit yanıt döndürür
- Token sayısını simüle eder
- Test için yeterli

Gerçek provider (sonra):
- Vercel AI SDK veya direkt HTTP
- Anthropic, OpenAI, Google Gemini

## Output
src/provider.ts oluşturuldu — LLMProvider interface + MockProvider

## Confidence
0.9

## Verification
- npx tsx src/provider.ts hatasız

## Status
done
