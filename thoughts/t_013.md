# Thought 013: Smoke Test — StateManager Entegrasyon

## Chain
chain_002_state

## Katman
Worker

## Input
"StateManager'ın tüm yeteneklerini test et: create, transition, persist, reload, invalid reject"

## Context Refs
- t_011 (temel yapı)
- t_012 (persist)

## Muhakeme
Unit test framework kurmadan önce basit bir smoke test yazacağım.
Assert ile temel senaryoları doğrulayacağım:

1. Fresh create → idle state
2. Valid transition: idle → visioning → decomposing → executing → verifying → complete
3. Invalid transition: idle → executing → REJECT
4. Missing reason → REJECT
5. Save → dosya var mı kontrol
6. Load → aynı state geri geliyor mu
7. History → geçişler kaydediliyor mu
8. Token tracking → addTokens çalışıyor mu

Bu test çalışırsa state machine hazır demektir.

## Output
src/state.test.ts oluşturuldu ve başarıyla çalıştı

## Confidence
0.9

## Verification
- npx tsx src/state.test.ts → tüm assertler geçer

## Status
done
