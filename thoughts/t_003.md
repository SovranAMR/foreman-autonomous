# Thought 003: Thought Interface

## Chain
chain_001_types

## Katman
Worker

## Input
"Sistemin atom birimi olan Thought'u TypeScript interface olarak tanımla"

## Context Refs
- t_001 (Layer)
- t_002 (ThoughtStatus, VerificationMethod)
- ARCHITECTURE.md → Thought interface

## Muhakeme
Thought, Foreman'ın en kritik tipi. Her şey bundan inşa ediliyor.
ARCHITECTURE.md'deki tanımı baz alacağım ama birkaç düzeltme var:

1. `contextRefs` sadece thought id listesi mi olmalı yoksa
   harici referanslar da (dosya, URL) içermeli mi?
   → İkisini de desteklemeli. string[] yeterli, 
   "t_001" veya "file:src/types.ts" veya "url:..." formatında.

2. `tokenCost` opsiyonel ama takip için önemli. 
   Rate limiter bütçe kontrolü için bunu kullanacak.

3. `model` hangi LLM kullanıldığını kaydeder. Debug için şart.

4. `reasoning` ZORUNLU string ama boş string kabul edilmemeli.
   Bunu runtime'da enforce edeceğiz, tip seviyesinde string yeterli.

5. `output` da ZORUNLU. Bir düşüncenin output'u yoksa tamamlanmamıştır.

6. `createdAt` ve `completedAt` ISO string. Date nesnesi yerine
   string — serileştirme basit, dosyaya yazma kolay.

## Output
types.ts'e Thought interface eklendi

## Confidence
0.95

## Verification
- npx tsx src/types.ts hatasız

## Status
done
