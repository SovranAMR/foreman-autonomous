# Thought 015: ChainManager — Create, Get, AddThought, UpdateStatus

## Chain
chain_003_persistence

## Katman
Worker

## Input
"Chain nesnelerini dosya sisteminde yöneten ChainManager sınıfını yaz"

## Context Refs
- t_004 (Chain interface)
- t_014 (ThoughtManager — chain'ler thought referansları tutuyor)

## Muhakeme
ChainManager, ThoughtManager'a paralel — chain'leri JSON olarak yönetir.
chains/chain_xxx.json formatında.

ThoughtManager'dan farklar:
1. ID üretimi farklı — chain_001_name formatında, user-specified olabilir
2. addThought() methodu — chain'e thought eklemek
3. Status hesaplaması — thought'ların durumuna göre chain status güncellenebilir

Tasarım kararları:

1. ID: auto mı manual mı?
   → İkisi de. create() hem auto hem manual id kabul etsin.
   → Auto: chain_001, chain_002, ...
   → Manual: user verir (chain_001_types gibi)

2. addThought() ne yapmalı?
   → Chain.thoughts listesine thought id ekler
   → Dosyayı günceller
   → Thought'un kendisini oluşturmaz — o ThoughtManager'ın işi

3. contextSummary ne zaman güncellenir?
   → Manuel update() ile. Otomatik özet üretimi engine'in işi.

## Output
src/chain-manager.ts oluşturuldu

## Confidence
0.9

## Verification
- npx tsx src/chain-manager.ts hatasız

## Status
done
