# Thought 005: SystemState + Geçiş Kuralları

## Chain
chain_001_types

## Katman
Worker

## Input
"Foreman'ın state machine'ini tanımla — durumlar ve geçerli geçişler"

## Context Refs
- t_001..t_004 (önceki tipler)
- ARCHITECTURE.md → State Machine bölümü

## Muhakeme
State machine Foreman'ın disiplin kaynağı. İşler iç içe geçmemeli —
sistem her an TEK BİR durumda olmalı.

ARCHITECTURE.md'deki tanımı baz alıyorum ama bir değişiklik:
geçiş kurallarını sadece Record<State, State[]> olarak tutmak yetmez.
Geçiş koşulları da olmalı — ama bu runtime mantığı, tip seviyesinde
sadece yapıyı tanımlamak yeterli. Koşulları state.ts'de implement edeceğiz.

"awaiting_human" durumu önemli — Foreman otomasyonla çalışır ama
bazı noktalarda (vizyon onayı, tehlikeli operasyon) insan onayı ister.
Bu, Foreman'ı "kontrolsüz AI" olmaktan çıkarır.

Bir ek durum düşündüm: "cooldown" — rate limit sonrası bekleme.
Ama bu state machine'e değil, rate limiter'a ait. State machine
iş akışını, rate limiter teknik kısıtlamaları yönetir. Ayrı tutmak doğru.

## Output
types.ts'e SystemState + VALID_TRANSITIONS eklendi

## Confidence
0.9

## Verification
- npx tsx src/types.ts hatasız
- Her state'in en az 1 geçişi var (dead state yok)

## Status
done
